import 'package:get/get.dart';

// This class is same as the getxcontroller
// It has all the lifecycle methods
// Get notifies the dependency injection system,
// that you cannot remove this service from the memory when it's not in use.

class Service extends GetxService {
  Future<void> increment() async{
    // Create the function body here
  }
}